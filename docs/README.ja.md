# worktree-tui

[English](../README.md) | 日本語

Git worktreeを管理するためのターミナルユーザーインターフェース（TUI）ツール

## 概要

worktree-tuiは、React + OpenTUIを使用した、git worktreeの操作をサポートするターミナルベースのインタラクティブツールです。[git-worktree-runner](https://github.com/coderabbitai/git-worktree-runner)をベースに、TUIによる直感的な操作を提供します。

## 特徴

- 📋 **Worktree一覧表示** - リポジトリ内の全worktreeをビジュアルに表示
- 🔄 **Worktree間の移動** - 選択したworktreeへのシームレスな切り替え
- 🛠️ **Build/Runコマンド実行** - ビルド、テスト、実行などのコマンドをworktree上で実行
- 📝 **Git操作サポート** - status、add、commit、push、pullなどの基本操作
- 🔗 **git-worktree-runner連携** - gtrコマンドとの統合

## 必要要件

- Bun 1.3.5+
- Zig 0.15.1+（OpenTUIビルド用）
- Git 2.22+
- [git-worktree-runner](https://github.com/coderabbitai/git-worktree-runner) (gtr) インストール済み
- 対応OS: macOS, Linux, WSL2

## インストール

```bash
# リポジトリをクローン
git clone https://github.com/r0227n/worktree-tui.git
cd worktree-tui

# 依存関係のインストール
bun install
```

## 使い方

リポジトリのルートディレクトリで以下のコマンドを実行：

```bash
bun run start
```

## キーボードショートカット

### グローバル

| キー | 動作 |
|------|------|
| `↑` / `↓` | worktree選択移動 |
| `Enter` | 選択したworktreeに移動 |
| `Tab` | 次のworktreeに移動 |
| `Esc` | メニュー/ダイアログを閉じる |
| `Ctrl+C` | アプリケーション終了 |
| `?` | ヘルプ表示 |

### Worktree操作

| キー | 動作 |
|------|------|
| `n` | 新規worktree作成 |
| `d` | 選択worktreeを削除 |
| `r` | worktreeリフレッシュ |

### 開発操作

| キー | 動作 |
|------|------|
| `b` | ビルド実行 |
| `t` | テスト実行 |
| `s` | 開発サーバー起動 |
| `e` | エディタで開く |

### Git操作

| キー | 動作 |
|------|------|
| `g s` | git status |
| `g a` | git add (ステージング) |
| `g c` | git commit |
| `g p` | git push |
| `g l` | git log |

## UI概要

```
┌─ Git Worktree Manager ──────────────────────────────────────────┐
│                                                                  │
│  Current: ~/project/main (main) ✓                                │
│                                                                  │
├─ Worktrees ─────────────────────────────────────────────────────┤
│  ▶ main          ~/project/main              [clean]            │
│    feature-auth  ~/project-worktrees/feat..  [2 changes]        │
│    hotfix-123    ~/project-worktrees/hotf..  [uncommitted]      │
│                                                                  │
├─ Actions ───────────────────────────────────────────────────────┤
│  [n] New  [d] Delete  [e] Editor  [g] Git  [b] Build  [?] Help  │
│                                                                  │
├─ Status ────────────────────────────────────────────────────────┤
│  Branch: main | Ahead: 0 | Behind: 0 | Modified: 0 files        │
└──────────────────────────────────────────────────────────────────┘
```

## 開発

### ビルド

```bash
bun run build
```

### テスト

```bash
bun test
```

### 開発サーバー

```bash
bun run dev
```

## 技術スタック

- **フレームワーク**: React + [OpenTUI](https://github.com/sst/opentui)
- **言語**: TypeScript
- **Git操作**: [simple-git](https://github.com/steveukx/git-js)
- **ランタイム**: Bun

## ドキュメント

詳細な仕様については、[spec.md](./spec.md)を参照してください。

## ライセンス

MIT License - 詳細は[LICENSE](./LICENSE)ファイルを参照してください。

## コントリビューション

プルリクエストを歓迎します！大きな変更の場合は、まずissueを開いて変更内容を議論してください。

## 作者

Ryo24 ([@r0227n](https://github.com/r0227n))

## 謝辞

- [OpenTUI](https://github.com/sst/opentui) - React製TUIフレームワーク
- [git-worktree-runner](https://github.com/coderabbitai/git-worktree-runner) - worktree管理CLI
- [simple-git](https://github.com/steveukx/git-js) - Git操作ライブラリ
